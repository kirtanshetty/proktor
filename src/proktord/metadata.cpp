#include <fcntl.h>
#include <metadata.h>

void dump_process_list(void* buf){
  // int fd = open(PK_METADATA_FILE, O_CREAT);
  // flock(fd, LOCK_EX);

  // unlink(lockfile);
  // flock(fd, LOCK_UN);
}

void get_process_list(void* buf){

}